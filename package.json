{
  "name": "playground",
  "source": "src/index.html",
  "version": "1.0.0",
  "description": "Ey",
  "scripts": {
      "build": "npx parcel build",
	"start": "npx parcel"
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
